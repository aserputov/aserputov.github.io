<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://aserputov.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://aserputov.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-21T20:58:08+00:00</updated><id>https://aserputov.github.io/feed.xml</id><title type="html">Anatoliy Serputov</title><subtitle>From-scratch implementations of neural networks and LLM inference optimizations. </subtitle><entry><title type="html">Mixture of Experts: 8x Parameters, Same Compute</title><link href="https://aserputov.github.io/blog/2026/moe-from-scratch/" rel="alternate" type="text/html" title="Mixture of Experts: 8x Parameters, Same Compute"/><published>2026-06-21T00:00:00+00:00</published><updated>2026-06-21T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/moe-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/moe-from-scratch/"><![CDATA[<h2 id="the-problem-with-dense-models">The Problem with Dense Models</h2> <p>In a standard transformer, every token passes through every parameter. A 47B model does 47B operations per token. Want better quality? Make the model bigger. But bigger = slower.</p> <p>Most of those operations are wasted — a token about math doesn’t need the weights that handle poetry. But a dense model has no choice: everything goes through everything.</p> <h2 id="the-idea">The Idea</h2> <p>Replace the single FFN with multiple smaller FFNs (experts) and a router that picks which ones to use:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dense:   token → Attention → FFN → output
                              ↑
                         all parameters

MoE:     token → Attention → Router → Expert 2,5 → output
                              ↑              ↑
                         picks 2 of 8    only 2 compute
</code></pre></div></div> <p>8 experts = 8x the parameters (knowledge), but each token only uses 2 = same compute per token.</p> <h2 id="router">Router</h2> <p>The router is one linear layer:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">router</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">768</span><span class="p">,</span> <span class="mi">8</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>  <span class="c1"># hidden → 8 scores
</span></code></pre></div></div> <p>For each token, it produces 8 scores, takes top-2, normalizes their weights, and routes:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">router_probs</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="nf">router</span><span class="p">(</span><span class="n">hidden_state</span><span class="p">))</span>     <span class="c1"># [0.05, 0.35, 0.1, 0.3, ...]
</span><span class="n">top_k_indices</span> <span class="o">=</span> <span class="nf">topk</span><span class="p">(</span><span class="n">router_probs</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>          <span class="c1"># experts 1 and 3
</span><span class="n">top_k_weights</span> <span class="o">=</span> <span class="nf">normalize</span><span class="p">([</span><span class="mf">0.35</span><span class="p">,</span> <span class="mf">0.3</span><span class="p">])</span>           <span class="c1"># [0.54, 0.46]
</span>
<span class="n">output</span> <span class="o">=</span> <span class="mf">0.54</span> <span class="o">*</span> <span class="nf">expert_1</span><span class="p">(</span><span class="n">token</span><span class="p">)</span> <span class="o">+</span> <span class="mf">0.46</span> <span class="o">*</span> <span class="nf">expert_3</span><span class="p">(</span><span class="n">token</span><span class="p">)</span>
</code></pre></div></div> <p>The router learns through backprop — no manual assignment of “this expert handles math.”</p> <h2 id="experts">Experts</h2> <p>Each expert is an identical FeedForward network:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">self</span><span class="p">.</span><span class="n">experts</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">ModuleList</span><span class="p">([</span>
    <span class="nc">FeedForward</span><span class="p">(</span><span class="n">d_model</span><span class="p">)</span> <span class="k">for</span> <span class="n">_</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">8</span><span class="p">)</span>
<span class="p">])</span>
</code></pre></div></div> <p>They start identical and specialize during training. One might learn to handle numbers, another syntax, another rare words — but these aren’t human-interpretable categories.</p> <h2 id="load-balancing">Load Balancing</h2> <p>Without a penalty, the router sends everything to 1-2 experts and ignores the rest. The load balancing loss keeps usage even:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># fraction: what % of tokens each expert got
# avg_prob: average router probability for each expert
</span><span class="n">balance_loss</span> <span class="o">=</span> <span class="n">num_experts</span> <span class="o">*</span> <span class="p">(</span><span class="n">fraction</span> <span class="o">*</span> <span class="n">avg_prob</span><span class="p">).</span><span class="nf">sum</span><span class="p">()</span>
</code></pre></div></div> <p>Minimum when all experts get equal traffic (~12.5% each for 8 experts). Added to the main loss with a small coefficient.</p> <h2 id="per-token-not-per-request">Per-Token, Not Per-Request</h2> <p>Routing happens at the token level, not the sequence level. Within one sentence, “cat” might go to expert 3 while “jumped” goes to expert 5. Different tokens need different processing — even within the same topic.</p> <h2 id="results">Results</h2> <p>Trained a small MoE (d=256, 4 layers, 8 experts) vs equivalent dense model:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Dense:  16.3M params, all active        loss: 0.33
MoE:    31.0M params, ~18.4M active     loss: 0.45

MoE has 1.9x params but ~1.1x active compute
Router usage: ~12-13% per expert (balanced)
</code></pre></div></div> <p>At this small scale, MoE overhead from routing slightly hurts. The benefit shows at scale:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mixtral 8x7B:      47B total, 13B active  ≈  LLaMA 2 70B quality
DeepSeek-V3:       256 experts, top-8     →  3% of params active per token
</code></pre></div></div> <h2 id="the-trend">The Trend</h2> <p>More experts, each smaller:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Mixtral (2023):      8 experts,  top-2  → 25% active
DeepSeek-V2 (2024):  160 experts, top-6 → 4% active
DeepSeek-V3 (2024):  256 experts, top-8 → 3% active
</code></pre></div></div> <p>Finer-grained experts = less wasted compute. The limit: too many tiny experts and the router can’t pick accurately, plus routing overhead grows.</p> <h2 id="why-this-matters">Why This Matters</h2> <p>GPT-4 is widely believed to be a MoE model. Most frontier models use some form of MoE — it’s the only way to scale knowledge (parameters) without proportionally scaling inference cost (compute). Understanding MoE is understanding how modern LLMs achieve their capability while remaining servable.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/moe-from-scratch">github.com/aserputov/moe-from-scratch</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><summary type="html"><![CDATA[How MoE replaces the dense FFN with multiple experts and a learned router, achieving large model quality at small model speed.]]></summary></entry><entry><title type="html">RLHF: From Text Predictor to Assistant</title><link href="https://aserputov.github.io/blog/2026/rlhf-from-scratch/" rel="alternate" type="text/html" title="RLHF: From Text Predictor to Assistant"/><published>2026-06-21T00:00:00+00:00</published><updated>2026-06-21T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/rlhf-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/rlhf-from-scratch/"><![CDATA[<h2 id="the-problem">The Problem</h2> <p>GPT-2 predicts the next token. Ask it “What is 2+2?” and it continues:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Q: What is 2+2?
Q: What is 3+3?
Q: What is 4+4?
</code></pre></div></div> <p>It doesn’t answer — it continues, because that’s what internet text looks like. SFT (supervised fine-tuning) teaches the format: question → answer. But the model still generates wordy, hedging, wrong answers. How do you define “good answer” as a loss function?</p> <p>You can’t. But humans can look at two answers and say which is better. RLHF turns that human judgment into a training signal.</p> <h2 id="three-steps">Three Steps</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPT-2 (base) → Reward Model → PPO → GPT-2 (assistant)
</code></pre></div></div> <p><strong>Step 1: Reward Model</strong> — a model that scores text quality as a single number.</p> <p><strong>Step 2: Human Preferences</strong> — train the reward model on pairs: “this answer is better than that one.”</p> <p><strong>Step 3: PPO</strong> — optimize the language model to generate text that gets high reward scores.</p> <h2 id="reward-model">Reward Model</h2> <p>Same GPT-2 architecture, but instead of predicting the next token, it outputs one number:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">RewardModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">base_model</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">base</span> <span class="o">=</span> <span class="n">base_model</span>                           <span class="c1"># GPT-2
</span>        <span class="n">self</span><span class="p">.</span><span class="n">reward_head</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="mi">768</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>             <span class="c1"># hidden → scalar
</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">input_ids</span><span class="p">):</span>
        <span class="n">hidden</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">base</span><span class="p">.</span><span class="nf">get_hidden</span><span class="p">(</span><span class="n">input_ids</span><span class="p">)</span>         <span class="c1"># all layers
</span>        <span class="n">last_hidden</span> <span class="o">=</span> <span class="n">hidden</span><span class="p">[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span>                   <span class="c1"># last token
</span>        <span class="k">return</span> <span class="n">self</span><span class="p">.</span><span class="nf">reward_head</span><span class="p">(</span><span class="n">last_hidden</span><span class="p">).</span><span class="nf">squeeze</span><span class="p">(</span><span class="o">-</span><span class="mi">1</span><span class="p">)</span>  <span class="c1"># one number
</span></code></pre></div></div> <p>The last token’s hidden state has seen the entire sequence (through causal attention), so it summarizes the whole text.</p> <h2 id="training-on-preferences">Training on Preferences</h2> <p>Humans rank pairs of answers. The loss pushes the reward model to score chosen answers higher than rejected ones:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">r_chosen</span> <span class="o">=</span> <span class="nf">reward_model</span><span class="p">(</span><span class="n">chosen_ids</span><span class="p">)</span>      <span class="c1"># score for good answer
</span><span class="n">r_rejected</span> <span class="o">=</span> <span class="nf">reward_model</span><span class="p">(</span><span class="n">rejected_ids</span><span class="p">)</span>  <span class="c1"># score for bad answer
</span>
<span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="nf">log_sigmoid</span><span class="p">(</span><span class="n">r_chosen</span> <span class="o">-</span> <span class="n">r_rejected</span><span class="p">)</span>
</code></pre></div></div> <p>This is the Bradley-Terry model — the same math used in chess Elo ratings. After training:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"Q: What is 2+2? A: 4."                    → score: -0.7
"Q: What is 2+2? A: Well, it could be..."  → score: -8.2
</code></pre></div></div> <p>The reward model learned: concise, correct answers score higher.</p> <h2 id="ppo-proximal-policy-optimization">PPO (Proximal Policy Optimization)</h2> <p>The RL loop that optimizes the language model:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">step</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="n">n_steps</span><span class="p">):</span>
    <span class="c1"># 1. Generate response
</span>    <span class="n">response</span> <span class="o">=</span> <span class="n">policy_model</span><span class="p">.</span><span class="nf">generate</span><span class="p">(</span><span class="n">prompt</span><span class="p">)</span>

    <span class="c1"># 2. Score it
</span>    <span class="n">reward</span> <span class="o">=</span> <span class="nf">reward_model</span><span class="p">(</span><span class="n">prompt</span> <span class="o">+</span> <span class="n">response</span><span class="p">)</span>

    <span class="c1"># 3. Compute how much policy changed
</span>    <span class="n">current_log_probs</span> <span class="o">=</span> <span class="n">policy_model</span><span class="p">.</span><span class="nf">log_prob</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>
    <span class="n">old_log_probs</span> <span class="o">=</span> <span class="n">old_policy</span><span class="p">.</span><span class="nf">log_prob</span><span class="p">(</span><span class="n">response</span><span class="p">)</span>
    <span class="n">ratio</span> <span class="o">=</span> <span class="nf">exp</span><span class="p">(</span><span class="n">current</span> <span class="o">-</span> <span class="n">old</span><span class="p">)</span>

    <span class="c1"># 4. Clipped objective (don't change too much per step)
</span>    <span class="n">clipped</span> <span class="o">=</span> <span class="nf">clamp</span><span class="p">(</span><span class="n">ratio</span><span class="p">,</span> <span class="mi">1</span><span class="o">-</span><span class="n">eps</span><span class="p">,</span> <span class="mi">1</span><span class="o">+</span><span class="n">eps</span><span class="p">)</span>
    <span class="n">loss</span> <span class="o">=</span> <span class="o">-</span><span class="nf">min</span><span class="p">(</span><span class="n">ratio</span> <span class="o">*</span> <span class="n">reward</span><span class="p">,</span> <span class="n">clipped</span> <span class="o">*</span> <span class="n">reward</span><span class="p">)</span>

    <span class="c1"># 5. KL penalty (don't forget how to speak)
</span>    <span class="n">kl</span> <span class="o">=</span> <span class="n">current_log_probs</span> <span class="o">-</span> <span class="n">ref_log_probs</span>
    <span class="n">loss</span> <span class="o">+=</span> <span class="n">kl_coeff</span> <span class="o">*</span> <span class="n">kl</span>
</code></pre></div></div> <p>Two safety mechanisms:</p> <p><strong>Clipping</strong> — limits how much the policy changes per step. Without it, the model could jump to degenerate behavior in one update.</p> <p><strong>KL penalty</strong> — penalizes divergence from the original model. Without it, the model would “reward hack” — find degenerate text that scores high on the reward model but is nonsensical. The KL penalty keeps it close to real language.</p> <h2 id="results">Results</h2> <p>On GPT-2 124M, the pipeline works but results are modest — the model is too small for real Q&amp;A:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Reward model:  100% accuracy on preference pairs (8 pairs, 20 epochs)
PPO:           50 steps, reward improved on trained prompts

Before RLHF:  "The capital of France. Q: What is the capi..."  [reward: 3.7]
After RLHF:   "The capital of France is Paris."                 [reward: 9.0]
</code></pre></div></div> <p>In production (InstructGPT paper, OpenAI 2022):</p> <ul> <li>GPT-3 175B as base</li> <li>40 human labelers writing ideal answers (SFT)</li> <li>33K comparison pairs for reward model</li> <li>PPO with KL penalty against SFT model</li> </ul> <p>Result: InstructGPT <strong>1.3B</strong> was preferred over base GPT-3 <strong>175B</strong> by human raters. RLHF is so effective that a small aligned model beats a large unaligned one.</p> <h2 id="why-this-matters">Why This Matters</h2> <p>Every major LLM assistant (ChatGPT, Claude, Gemini) uses some form of RLHF or its successors (DPO, RLAIF). The base model learns language from internet text. RLHF teaches it to be helpful, honest, and harmless — properties that can’t be captured by next-token prediction alone.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/rlhf-from-scratch">github.com/aserputov/rlhf-from-scratch</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><summary type="html"><![CDATA[How RLHF turns a language model into an assistant — reward model training on human preferences, PPO optimization, and the full pipeline from scratch.]]></summary></entry><entry><title type="html">LoRA: Fine-Tuning 0.12% of the Parameters</title><link href="https://aserputov.github.io/blog/2026/lora-from-scratch/" rel="alternate" type="text/html" title="LoRA: Fine-Tuning 0.12% of the Parameters"/><published>2026-06-20T00:00:00+00:00</published><updated>2026-06-20T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/lora-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/lora-from-scratch/"><![CDATA[<h2 id="the-problem-with-full-fine-tuning">The Problem with Full Fine-Tuning</h2> <p>GPT-2 has 124M parameters. Fine-tuning all of them means:</p> <ul> <li><strong>Backward pass</strong> computes gradients for 124M params</li> <li><strong>Adam optimizer</strong> stores 2 extra copies per param (momentum + variance) = 3x model size in memory</li> <li><strong>Storage</strong>: every fine-tuned version is a full 500MB checkpoint</li> <li><strong>Serving</strong>: swapping between tasks means loading entirely different models</li> </ul> <p>For one task that’s manageable. For 100 tasks (customer support, translation, summarization, code…) it’s 50GB of checkpoints and no way to hot-swap.</p> <h2 id="the-idea">The Idea</h2> <p>Most of the knowledge is already in the pretrained weights. Fine-tuning only adjusts behavior slightly. The weight update matrix <code class="language-plaintext highlighter-rouge">delta_W</code> during fine-tuning is empirically <strong>low-rank</strong> — it can be decomposed into two small matrices.</p> <p>Instead of updating the full weight matrix W (768x768), add a parallel branch:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>                    ┌─────────┐
              ┌────►│  W      │────┐
              │     │ (frozen)│    │
    x ────────┤     └─────────┘    ├────► output
              │     ┌────┐ ┌────┐ │
              └────►│ A  │►│ B  │─┘
                    │4×768│ │768×4│
                    └────┘ └────┘
</code></pre></div></div> <p>W stays frozen. A and B are the only trainable parameters. The forward pass computes <code class="language-plaintext highlighter-rouge">W(x) + B(A(x)) * alpha</code>.</p> <h2 id="why-q-and-v-only">Why Q and V Only</h2> <p>In each transformer block, the combined QKV projection <code class="language-plaintext highlighter-rouge">c_attn</code> maps 768 → 2304 (three copies of 768 for Q, K, V). We add LoRA to:</p> <ul> <li><strong>Q slice</strong> (0:768) — what the token is looking for</li> <li><strong>V slice</strong> (1536:2304) — what the token provides as content</li> </ul> <p>K is skipped. The original LoRA paper found that Q+V gives the best quality per parameter. Adding K doesn’t help much because Q and K are mathematically symmetric in the attention score — adjusting Q already changes which keys get attended to.</p> <h2 id="the-math">The Math</h2> <p>For rank r=4, d_model=768, applied to Q and V in 12 blocks:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Per block:  2 × (A: 768×4 + B: 4×768) = 2 × 6,144 = 12,288 params
Total:      12 blocks × 12,288 = 147,456 trainable params

Base model: 124,587,264 params (frozen)
LoRA:       147,456 params (trainable)
Ratio:      0.12%
</code></pre></div></div> <h2 id="where-the-savings-come-from">Where the Savings Come From</h2> <p>The forward pass is the same cost — all 124M params are used. LoRA saves on everything else:</p> <p><strong>Backward pass</strong>: gradients computed only for 147K params instead of 124M. The chain rule still flows through frozen layers (to compute gradients for LoRA params deeper in the network), but no weight updates are stored for frozen params.</p> <p><strong>Optimizer memory</strong>: Adam stores momentum and variance per trainable param. Full fine-tuning: 124M × 3 × 4 bytes ≈ 1.5GB. LoRA: 147K × 3 × 4 bytes ≈ 1.7MB.</p> <p><strong>Storage</strong>: full checkpoint is 500MB. LoRA adapter is 576KB. You can store 850 different adapters in the space of one full model.</p> <p><strong>Serving</strong>: base model stays in memory. Swap adapters by loading 576KB — instant task switching.</p> <h2 id="implementation">Implementation</h2> <p>The <code class="language-plaintext highlighter-rouge">LoRALinear</code> wrapper keeps the original layer frozen and adds A,B pairs for each target slice:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">LoRALinear</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">original_linear</span><span class="p">,</span> <span class="n">rank</span><span class="o">=</span><span class="mi">4</span><span class="p">,</span> <span class="n">alpha</span><span class="o">=</span><span class="mf">1.0</span><span class="p">,</span> <span class="n">target_slices</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">original</span> <span class="o">=</span> <span class="n">original_linear</span>
        <span class="n">self</span><span class="p">.</span><span class="n">rank</span> <span class="o">=</span> <span class="n">rank</span>
        <span class="n">self</span><span class="p">.</span><span class="n">alpha</span> <span class="o">=</span> <span class="n">alpha</span>

        <span class="c1"># For GPT-2's combined QKV: target_slices = [(0, 768), (1536, 2304)]
</span>        <span class="k">for</span> <span class="n">start</span><span class="p">,</span> <span class="n">end</span> <span class="ow">in</span> <span class="n">target_slices</span><span class="p">:</span>
            <span class="n">out_dim</span> <span class="o">=</span> <span class="n">end</span> <span class="o">-</span> <span class="n">start</span>
            <span class="n">A</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">in_features</span><span class="p">,</span> <span class="n">rank</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>     <span class="c1"># 768 → 4
</span>            <span class="n">B</span> <span class="o">=</span> <span class="n">nn</span><span class="p">.</span><span class="nc">Linear</span><span class="p">(</span><span class="n">rank</span><span class="p">,</span> <span class="n">out_dim</span><span class="p">,</span> <span class="n">bias</span><span class="o">=</span><span class="bp">False</span><span class="p">)</span>          <span class="c1"># 4 → 768
</span>            <span class="n">nn</span><span class="p">.</span><span class="n">init</span><span class="p">.</span><span class="nf">zeros_</span><span class="p">(</span><span class="n">B</span><span class="p">.</span><span class="n">weight</span><span class="p">)</span>  <span class="c1"># start with zero delta
</span>
    <span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">):</span>
        <span class="n">base_out</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">original</span><span class="p">(</span><span class="n">x</span><span class="p">)</span>           <span class="c1"># frozen W(x)
</span>        <span class="nf">for </span><span class="p">(</span><span class="n">start</span><span class="p">,</span> <span class="n">end</span><span class="p">),</span> <span class="p">(</span><span class="n">A</span><span class="p">,</span> <span class="n">B</span><span class="p">)</span> <span class="ow">in</span> <span class="nf">zip</span><span class="p">(...):</span>
            <span class="n">lora_out</span> <span class="o">=</span> <span class="nc">B</span><span class="p">(</span><span class="nc">A</span><span class="p">(</span><span class="n">x</span><span class="p">))</span> <span class="o">*</span> <span class="n">self</span><span class="p">.</span><span class="n">alpha</span>   <span class="c1"># low-rank delta
</span>            <span class="n">base_out</span><span class="p">[:,</span> <span class="p">:,</span> <span class="n">start</span><span class="p">:</span><span class="n">end</span><span class="p">]</span> <span class="o">+=</span> <span class="n">lora_out</span>
        <span class="k">return</span> <span class="n">base_out</span>
</code></pre></div></div> <p>B is initialized to zeros so at the start, LoRA has zero effect — the model behaves exactly like the original.</p> <h2 id="results">Results</h2> <p>Fine-tuned GPT-2 on Shakespeare quotes (1,111 tokens, 5 epochs, 12 seconds on M1):</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Before: "Shakespeare said: I think it's a good idea to have a lot
         of people who are not in the business of writing..."

After:  "Shakespeare said, 'A man must undergo the mind. He must
         suffer the slings and arrows, or he must suffer the bad fortune."
</code></pre></div></div> <p>Training loss: 3.58 → 0.24</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Base params:    124,587,264 (frozen)
LoRA params:    147,456 (trainable, 0.12%)
Adapter size:   576 KB (48 tensors)
Training time:  11.9s
</code></pre></div></div> <p>48 tensors = 2 pairs (A, B) x 2 slices (Q, V) x 12 transformer blocks.</p> <h2 id="why-this-matters-for-serving">Why This Matters for Serving</h2> <p>LoRA is how production LLMs handle multi-tenant fine-tuning. One base model in GPU memory, hundreds of adapters on disk. When a request comes in for “customer-support-v3”, load 576KB, add the delta to the forward pass, serve. Next request is “code-review-v2” — swap adapter, same base model.</p> <p>Combined with PagedAttention and continuous batching, you can serve different LoRA adapters in the same batch — each request gets its own adapter applied during the attention computation.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><summary type="html"><![CDATA[How LoRA freezes 124M base parameters and trains only 147K by adding low-rank A,B matrices alongside Q and V projections.]]></summary></entry><entry><title type="html">Speculative Decoding: Trading Draft Guesses for Parallel Verification</title><link href="https://aserputov.github.io/blog/2026/speculative-decoding/" rel="alternate" type="text/html" title="Speculative Decoding: Trading Draft Guesses for Parallel Verification"/><published>2026-06-17T00:00:00+00:00</published><updated>2026-06-17T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/speculative-decoding</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/speculative-decoding/"><![CDATA[<h2 id="why-decode-is-slow">Why Decode is Slow</h2> <p>Autoregressive generation produces one token per forward pass. Each token waits for the previous one. With KV-cache, we avoid recomputing past tokens, but the new token still passes through every layer of the model.</p> <p>The problem: during decode of a single token, the GPU is loaded at maybe 5-10%. Most of the time is spent loading model weights from memory, not computing. The GPU has thousands of cores with almost nothing to do.</p> <h2 id="the-idea">The Idea</h2> <p>Use two models:</p> <ul> <li><strong>Draft model</strong> (small, fast): generates K candidate tokens sequentially</li> <li><strong>Target model</strong> (large, accurate): verifies all K candidates in one forward pass</li> </ul> <p>Verification is the same operation as prefill — all tokens are known, so the GPU processes them in parallel. One forward pass to check 5 tokens costs about the same as one forward pass to generate 1 token, because the bottleneck is loading weights, not computation.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Standard (target only, 5 tokens):
  tok1 → tok2 → tok3 → tok4 → tok5
  5 forward passes = 500ms

Speculative:
  Draft generates:  tok1, tok2, tok3, tok4, tok5  = 150ms (small model)
  Target verifies:  [all 5 at once]                = 100ms (one pass)
  Total: 250ms for 5 tokens (if all accepted)
</code></pre></div></div> <h2 id="acceptance-and-rejection">Acceptance and Rejection</h2> <p>The target model checks each draft token against what it would have generated:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">for</span> <span class="n">j</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="nf">len</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">)):</span>
    <span class="n">target_tok</span> <span class="o">=</span> <span class="nf">argmax</span><span class="p">(</span><span class="n">target_logits</span><span class="p">[</span><span class="n">j</span><span class="p">])</span>
    <span class="k">if</span> <span class="n">target_tok</span> <span class="o">==</span> <span class="n">draft_tokens</span><span class="p">[</span><span class="n">j</span><span class="p">]:</span>
        <span class="nf">accept</span><span class="p">(</span><span class="n">draft_tokens</span><span class="p">[</span><span class="n">j</span><span class="p">])</span>      <span class="c1"># draft guessed right
</span>    <span class="k">else</span><span class="p">:</span>
        <span class="nf">accept</span><span class="p">(</span><span class="n">target_tok</span><span class="p">)</span>           <span class="c1"># use target's choice
</span>        <span class="k">break</span>                        <span class="c1"># stop here, rest is invalid
</span></code></pre></div></div> <p>We accept tokens from the beginning until the first mismatch. After a wrong token, everything after it is contaminated — the K,V values depend on all previous tokens, so a wrong token means wrong context for everything downstream.</p> <p>If the draft model has 70% per-token accuracy and we draft 5 tokens, we typically accept 3-4 per iteration — that’s 3-4 tokens for the cost of one target forward pass.</p> <h2 id="why-this-doesnt-lose-quality">Why This Doesn’t Lose Quality</h2> <p>The target model makes every final decision. Draft tokens are only kept if the target agrees. The output is mathematically identical to what the target would produce on its own. The draft model is just guessing to save time — wrong guesses cost a little draft compute but nothing else.</p> <h2 id="implementation">Implementation</h2> <p>The draft model is a truncated version of the target — same architecture, fewer layers:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">DraftModel</span><span class="p">(</span><span class="n">nn</span><span class="p">.</span><span class="n">Module</span><span class="p">):</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">full_model</span><span class="p">,</span> <span class="n">n_draft_layers</span><span class="o">=</span><span class="mi">10</span><span class="p">):</span>
        <span class="nf">super</span><span class="p">().</span><span class="nf">__init__</span><span class="p">()</span>
        <span class="n">self</span><span class="p">.</span><span class="n">wte</span> <span class="o">=</span> <span class="n">full_model</span><span class="p">.</span><span class="n">wte</span>        <span class="c1"># shared embeddings
</span>        <span class="n">self</span><span class="p">.</span><span class="n">wpe</span> <span class="o">=</span> <span class="n">full_model</span><span class="p">.</span><span class="n">wpe</span>        <span class="c1"># shared positions
</span>        <span class="n">self</span><span class="p">.</span><span class="n">blocks</span> <span class="o">=</span> <span class="n">full_model</span><span class="p">.</span><span class="n">blocks</span><span class="p">[:</span><span class="n">n_draft_layers</span><span class="p">]</span>  <span class="c1"># fewer layers
</span>        <span class="n">self</span><span class="p">.</span><span class="n">ln_f</span> <span class="o">=</span> <span class="n">full_model</span><span class="p">.</span><span class="n">ln_f</span>      <span class="c1"># shared layer norm
</span></code></pre></div></div> <p>In production, the draft model is typically a separately trained small model (like a distilled version), which gives much better acceptance rates.</p> <h2 id="results">Results</h2> <p>On CPU (Apple M1), speculative decoding doesn’t show speedup because CPU processes 5 tokens ~5x slower than 1 — there’s no parallelism advantage. The speedup is GPU-specific, where verification of K tokens costs roughly the same as generating 1 token due to the memory-bound nature of decode.</p> <p>Published benchmarks on GPU:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Google (T5-XXL):     2-3x speedup
NVIDIA (H200):       3.6x speedup
AMD (MI300X):        1.5-2.9x speedup
</code></pre></div></div> <h2 id="why-its-memory-bound">Why It’s Memory-Bound</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Decode 1 token:   load 500MB of weights → multiply (1, 768) × (768, 768)
Decode 5 tokens:  load 500MB of weights → multiply (5, 768) × (768, 768)

Loading weights: ~95% of the time (same for both)
Compute:         ~5% of the time (5x more, but 5% × 5 = 25%, still small)
</code></pre></div></div> <p>The weights have to be loaded regardless. Speculative decoding makes the GPU do useful work during the time it would otherwise spend waiting.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><summary type="html"><![CDATA[How speculative decoding uses a small draft model to propose tokens and a large model to verify them in one pass, achieving 2-3x speedup on GPU.]]></summary></entry><entry><title type="html">Prefix Caching: Reusing KV Across Requests</title><link href="https://aserputov.github.io/blog/2026/prefix-caching/" rel="alternate" type="text/html" title="Prefix Caching: Reusing KV Across Requests"/><published>2026-06-14T00:00:00+00:00</published><updated>2026-06-14T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/prefix-caching</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/prefix-caching/"><![CDATA[<h2 id="the-problem">The Problem</h2> <p>Every ChatGPT-style request starts with the same system prompt:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"You are a helpful assistant that answers concisely..."  ← same for everyone
"What is Flash Attention?"                                ← unique per user
</code></pre></div></div> <p>Without prefix caching, every request runs prefill on the full prompt — including the system prompt that hasn’t changed. 100 users = 100 identical prefills of the same 500 tokens.</p> <h2 id="the-insight">The Insight</h2> <p>KV values for a prefix are deterministic. If the same tokens appear at the same positions, they produce the same K,V at every layer. This is because:</p> <ul> <li><strong>Layer 1</strong>: K,V depend on token embedding + position embedding. Same tokens, same positions = same K,V.</li> <li><strong>Layer 2+</strong>: K,V depend on the output of the previous layer, which mixed information from all previous tokens via attention. Same previous tokens = same mixed output = same K,V.</li> </ul> <p>This only works for <strong>prefixes</strong> — tokens from the beginning. You can’t cache “thanks for your help” from the middle of one conversation and reuse it in another, because by that point K,V depend on everything that came before.</p> <h2 id="implementation">Implementation</h2> <p>Built on top of PagedAttention. The prefix cache is a dict mapping token tuples to page table entries:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">PrefixCache</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="p">...):</span>
        <span class="c1"># Same page pool as PagedAttention
</span>        <span class="n">self</span><span class="p">.</span><span class="n">k_pool</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">max_pages</span><span class="p">,</span> <span class="n">n_heads</span><span class="p">,</span> <span class="n">page_size</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">v_pool</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">max_pages</span><span class="p">,</span> <span class="n">n_heads</span><span class="p">,</span> <span class="n">page_size</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">)</span>

        <span class="c1"># NEW: prefix → page table mapping
</span>        <span class="n">self</span><span class="p">.</span><span class="n">cache</span> <span class="o">=</span> <span class="p">{}</span>   <span class="c1"># hash(prefix_tokens) → page references
</span>
    <span class="k">def</span> <span class="nf">lookup</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">tokens</span><span class="p">):</span>
        <span class="c1"># Find longest matching prefix in cache
</span>        <span class="n">key</span> <span class="o">=</span> <span class="nf">tuple</span><span class="p">(</span><span class="n">tokens</span><span class="p">)</span>
        <span class="k">for</span> <span class="n">cached_key</span><span class="p">,</span> <span class="n">entry</span> <span class="ow">in</span> <span class="n">self</span><span class="p">.</span><span class="n">cache</span><span class="p">.</span><span class="nf">items</span><span class="p">():</span>
            <span class="n">match</span> <span class="o">=</span> <span class="nf">common_prefix_length</span><span class="p">(</span><span class="n">cached_key</span><span class="p">,</span> <span class="n">key</span><span class="p">)</span>
            <span class="k">if</span> <span class="k">match</span> <span class="o">&gt;</span> <span class="n">best</span><span class="p">:</span>
                <span class="n">best</span> <span class="o">=</span> <span class="k">match</span>
        <span class="k">return</span> <span class="n">best</span><span class="p">,</span> <span class="n">entry</span>
</code></pre></div></div> <p>On cache miss — prefill the prefix, save KV pages to cache, then prefill remaining tokens:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Cache MISS: first request with this system prompt
</span><span class="n">logits</span> <span class="o">=</span> <span class="nf">prefill</span><span class="p">(</span><span class="n">prefix_tokens</span><span class="p">,</span> <span class="n">start_pos</span><span class="o">=</span><span class="mi">0</span><span class="p">)</span>
<span class="n">prefix_cache</span><span class="p">.</span><span class="nf">save_prefix</span><span class="p">(</span><span class="n">prefix_tokens</span><span class="p">,</span> <span class="n">seq_id</span><span class="p">)</span>  <span class="c1"># save for future
</span><span class="n">logits</span> <span class="o">=</span> <span class="nf">prefill</span><span class="p">(</span><span class="n">question_tokens</span><span class="p">,</span> <span class="n">start_pos</span><span class="o">=</span><span class="nf">len</span><span class="p">(</span><span class="n">prefix_tokens</span><span class="p">))</span>
</code></pre></div></div> <p>On cache hit — clone page table pointers, prefill only the unique part:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Cache HIT: same system prompt seen before
</span><span class="n">prefix_cache</span><span class="p">.</span><span class="nf">clone_pages_from_entry</span><span class="p">(</span><span class="n">seq_id</span><span class="p">,</span> <span class="n">entry</span><span class="p">,</span> <span class="n">hit_len</span><span class="p">)</span>
<span class="n">logits</span> <span class="o">=</span> <span class="nf">prefill</span><span class="p">(</span><span class="n">question_tokens</span><span class="p">,</span> <span class="n">start_pos</span><span class="o">=</span><span class="n">hit_len</span><span class="p">)</span>  <span class="c1"># skip system prompt
</span></code></pre></div></div> <h2 id="why-hash-tokens-not-text">Why Hash Tokens, Not Text?</h2> <p>Different text can produce different tokens:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>"You are"     → [1639, 389]       # normal
"You  are"    → [1639, 220, 389]  # extra space = different tokens
</code></pre></div></div> <p>The model operates on tokens. KV is computed from tokens. So the cache key must be tokens.</p> <h2 id="benchmark-results">Benchmark Results</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>System prompt: 13 tokens | 5 questions | 30 tokens each

Without prefix caching:  4.72s  (31.8 tokens/sec)
With prefix caching:     4.41s  (34.0 tokens/sec)
Speedup:                 1.07x
Prefill tokens saved:    52 (system prompt computed once)
</code></pre></div></div> <p>The speedup is small because our system prompt is only 13 tokens. In production, system prompts are 500-2000 tokens — that’s where prefix caching saves significant compute. Anthropic’s prompt caching, for example, reduces costs by up to 90% on cached prefixes.</p> <h2 id="why-this-matters">Why This Matters</h2> <p>This is how every major API (Claude, GPT-4, Gemini) handles system prompts efficiently. Without it, serving costs would scale linearly with the number of requests sharing the same prefix. Understanding this connects PagedAttention (memory management) to real serving economics.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><summary type="html"><![CDATA[Why every request recomputes the same system prompt, and how prefix caching eliminates redundant prefill by sharing KV pages.]]></summary></entry><entry><title type="html">Flash Attention: Writing a GPU Kernel in Triton</title><link href="https://aserputov.github.io/blog/2026/flash-attention-from-scratch/" rel="alternate" type="text/html" title="Flash Attention: Writing a GPU Kernel in Triton"/><published>2026-06-13T00:00:00+00:00</published><updated>2026-06-13T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/flash-attention-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/flash-attention-from-scratch/"><![CDATA[<h2 id="the-ot-problem">The O(T²) Problem</h2> <p>Standard attention computes the full scores matrix:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">scores</span> <span class="o">=</span> <span class="n">Q</span> <span class="o">@</span> <span class="n">K</span><span class="p">.</span><span class="n">T</span>       <span class="c1"># T×T matrix — O(T²) memory
</span><span class="n">weights</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">scores</span><span class="p">)</span>
<span class="n">output</span> <span class="o">=</span> <span class="n">weights</span> <span class="o">@</span> <span class="n">V</span>
</code></pre></div></div> <p>For T=1024, that’s 1 million entries. For T=128K (modern LLMs), it’s 16 billion — doesn’t fit in any GPU.</p> <h2 id="the-key-insight">The Key Insight</h2> <p>We don’t need the full T×T matrix at once. We can compute attention in <strong>tiles</strong> — small blocks that fit in GPU SRAM (fast on-chip memory, ~20MB) instead of HBM (main GPU memory, ~8-80GB).</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Instead of:  compute entire 1024×1024 matrix
Do this:     compute 64×64 tiles, one at a time, accumulate result
</code></pre></div></div> <h2 id="online-softmax">Online Softmax</h2> <p>The trick is computing softmax incrementally. Normally softmax needs ALL scores. But with the online algorithm, we maintain a running max and running sum:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># Process K tiles one at a time
</span><span class="k">for</span> <span class="n">each</span> <span class="n">K_tile</span><span class="p">:</span>
    <span class="n">scores</span> <span class="o">=</span> <span class="n">Q_tile</span> <span class="o">@</span> <span class="n">K_tile</span><span class="p">.</span><span class="n">T</span>           <span class="c1"># 64×64 — fits in SRAM
</span>    <span class="n">m_new</span> <span class="o">=</span> <span class="nf">max</span><span class="p">(</span><span class="n">m_old</span><span class="p">,</span> <span class="nf">max</span><span class="p">(</span><span class="n">scores</span><span class="p">))</span>       <span class="c1"># update running max
</span>    <span class="n">alpha</span> <span class="o">=</span> <span class="nf">exp</span><span class="p">(</span><span class="n">m_old</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>            <span class="c1"># rescale factor
</span>    <span class="n">p</span> <span class="o">=</span> <span class="nf">exp</span><span class="p">(</span><span class="n">scores</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>               <span class="c1"># softmax numerator
</span>    <span class="n">acc</span> <span class="o">=</span> <span class="n">acc</span> <span class="o">*</span> <span class="n">alpha</span> <span class="o">+</span> <span class="n">p</span> <span class="o">@</span> <span class="n">V_tile</span>        <span class="c1"># rescale old + add new
</span>    <span class="n">l</span> <span class="o">=</span> <span class="n">l</span> <span class="o">*</span> <span class="n">alpha</span> <span class="o">+</span> <span class="nf">sum</span><span class="p">(</span><span class="n">p</span><span class="p">)</span>                <span class="c1"># update denominator
</span>
<span class="n">output</span> <span class="o">=</span> <span class="n">acc</span> <span class="o">/</span> <span class="n">l</span>  <span class="c1"># final normalization — identical to standard softmax
</span></code></pre></div></div> <h2 id="the-triton-kernel">The Triton Kernel</h2> <p><a href="https://github.com/triton-lang/triton">Triton</a> (by OpenAI) lets you write GPU kernels in Python that compile to PTX (NVIDIA assembly). Here’s the core of the kernel:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@triton.jit</span>
<span class="k">def</span> <span class="nf">_flash_attn_fwd</span><span class="p">(</span><span class="n">Q</span><span class="p">,</span> <span class="n">K</span><span class="p">,</span> <span class="n">V</span><span class="p">,</span> <span class="n">Out</span><span class="p">,</span> <span class="p">...):</span>
    <span class="c1"># Each GPU "thread group" processes one tile of queries
</span>    <span class="n">pid_m</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">program_id</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>   <span class="c1"># which Q tile (0..T/64)
</span>    <span class="n">pid_z</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">program_id</span><span class="p">(</span><span class="mi">1</span><span class="p">)</span>   <span class="c1"># which batch × head
</span>
    <span class="n">q</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">q_ptrs</span><span class="p">)</span>        <span class="c1"># load 64 rows of Q into SRAM
</span>    <span class="n">q</span> <span class="o">=</span> <span class="n">q</span> <span class="o">*</span> <span class="p">(</span><span class="mf">1.0</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">D</span><span class="p">))</span>

    <span class="c1"># Iterate over K,V tiles
</span>    <span class="k">for</span> <span class="n">start_n</span> <span class="ow">in</span> <span class="nf">range</span><span class="p">(</span><span class="mi">0</span><span class="p">,</span> <span class="n">causal_bound</span><span class="p">,</span> <span class="n">BLOCK_N</span><span class="p">):</span>
        <span class="n">kt</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">k_ptrs</span><span class="p">)</span>   <span class="c1"># load K tile (transposed)
</span>        <span class="n">s</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">q</span><span class="p">,</span> <span class="n">kt</span><span class="p">)</span>      <span class="c1"># 64×64 matmul in SRAM
</span>        <span class="n">s</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">where</span><span class="p">(</span><span class="n">causal_mask</span><span class="p">,</span> <span class="n">s</span><span class="p">,</span> <span class="o">-</span><span class="n">inf</span><span class="p">)</span>

        <span class="c1"># Online softmax
</span>        <span class="n">m_new</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">maximum</span><span class="p">(</span><span class="n">m_i</span><span class="p">,</span> <span class="n">tl</span><span class="p">.</span><span class="nf">max</span><span class="p">(</span><span class="n">s</span><span class="p">,</span> <span class="mi">1</span><span class="p">))</span>
        <span class="n">p</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">s</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span>
        <span class="n">v</span> <span class="o">=</span> <span class="n">tl</span><span class="p">.</span><span class="nf">load</span><span class="p">(</span><span class="n">v_ptrs</span><span class="p">)</span>
        <span class="n">acc</span> <span class="o">=</span> <span class="n">acc</span> <span class="o">*</span> <span class="n">tl</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">m_i</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span> <span class="o">+</span> <span class="n">tl</span><span class="p">.</span><span class="nf">dot</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>
        <span class="n">l_i</span> <span class="o">=</span> <span class="n">l_i</span> <span class="o">*</span> <span class="n">tl</span><span class="p">.</span><span class="nf">exp</span><span class="p">(</span><span class="n">m_i</span> <span class="o">-</span> <span class="n">m_new</span><span class="p">)</span> <span class="o">+</span> <span class="n">tl</span><span class="p">.</span><span class="nf">sum</span><span class="p">(</span><span class="n">p</span><span class="p">,</span> <span class="mi">1</span><span class="p">)</span>
        <span class="n">m_i</span> <span class="o">=</span> <span class="n">m_new</span>

    <span class="n">tl</span><span class="p">.</span><span class="nf">store</span><span class="p">(</span><span class="n">o_ptrs</span><span class="p">,</span> <span class="n">acc</span> <span class="o">/</span> <span class="n">l_i</span><span class="p">)</span>
</code></pre></div></div> <h2 id="gpu-benchmark-results-tesla-t4">GPU Benchmark Results (Tesla T4)</h2> <p><strong>Attention kernel (standard vs flash):</strong></p> <table> <thead> <tr> <th>Seq Length</th> <th>Standard</th> <th>Flash</th> <th>Match</th> </tr> </thead> <tbody> <tr> <td>128</td> <td>0.16ms</td> <td>0.30ms</td> <td>yes</td> </tr> <tr> <td>256</td> <td>0.32ms</td> <td>0.85ms</td> <td>yes</td> </tr> <tr> <td>512</td> <td>0.88ms</td> <td>2.16ms</td> <td>yes</td> </tr> <tr> <td>1024</td> <td>2.66ms</td> <td>7.78ms</td> <td>yes</td> </tr> </tbody> </table> <p>Flash is <strong>slower</strong> at GPT-2’s short context (max 1024) because the T×T matrix fits in GPU memory. The real win is <strong>memory</strong>:</p> <table> <thead> <tr> <th>Seq Length</th> <th>Standard</th> <th>Flash</th> <th>Savings</th> </tr> </thead> <tbody> <tr> <td>256</td> <td>3.0 MB</td> <td>0.188 MB</td> <td>16x</td> </tr> <tr> <td>512</td> <td>12.0 MB</td> <td>0.188 MB</td> <td>64x</td> </tr> <tr> <td>1024</td> <td>48.0 MB</td> <td>0.188 MB</td> <td>256x</td> </tr> </tbody> </table> <p>Flash tile is always 64×64 = constant memory, regardless of sequence length.</p> <p><strong>Full generation:</strong></p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GPU (Flash + KV-cache):  51.5 tokens/sec
CPU (KV-cache only):     18.2 tokens/sec
GPU speedup:             2.8x
</code></pre></div></div> <h2 id="model-design">Model Design</h2> <p>Flash Attention optimizes <strong>prefill</strong> (processing the full prompt). During decode, Q is just 1 token — no T×T matrix to tile. So the model uses:</p> <ul> <li><strong>Prefill</strong>: Flash Attention (Triton kernel)</li> <li><strong>Decode</strong>: Standard attention with KV-cache</li> </ul> <p>This matches what production serving systems do.</p> <h2 id="why-this-matters">Why This Matters</h2> <p>Every major LLM (GPT-4, Claude, Gemini) uses Flash Attention. Without it, long-context models (128K+ tokens) would be impossible — the attention matrix simply wouldn’t fit in memory. Understanding it from the kernel level up is fundamental to AI infrastructure work.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><category term="gpu"/><summary type="html"><![CDATA[How Flash Attention avoids materializing the O(T²) attention matrix by tiling Q×K^T in GPU SRAM, with a from-scratch Triton kernel.]]></summary></entry><entry><title type="html">PagedAttention: Virtual Memory for KV-Cache</title><link href="https://aserputov.github.io/blog/2026/pagedattention-from-scratch/" rel="alternate" type="text/html" title="PagedAttention: Virtual Memory for KV-Cache"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/pagedattention-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/pagedattention-from-scratch/"><![CDATA[<h2 id="the-hidden-cost-of-torchcat">The Hidden Cost of torch.cat</h2> <p>KV-cache gives us a 2.6x speedup by caching Key and Value matrices. But the naive implementation using <code class="language-plaintext highlighter-rouge">torch.cat</code> has a problem:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">k</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">prev_k</span><span class="p">,</span> <span class="n">k</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>  <span class="c1"># creates NEW tensor, copies ALL previous data
</span></code></pre></div></div> <p>Every step:</p> <ol> <li>Allocate a new tensor (old size + 1)</li> <li>Copy the entire old cache into it</li> <li>Add the one new token</li> <li>Deallocate the old tensor</li> </ol> <p>At step 100, we copy 99 tokens to add 1. At step 1000, we copy 999. Total copies: 1 + 2 + … + n = <strong>O(n²)</strong>.</p> <h2 id="pagedattention-the-os-analogy">PagedAttention: The OS Analogy</h2> <p>Operating systems solved this decades ago with virtual memory. Instead of contiguous allocation, they use <strong>pages</strong> — fixed-size blocks mapped through a page table.</p> <p>PagedAttention applies the same idea to KV-cache:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>┌─────────────────────────────────────┐
│  Page Pool (pre-allocated)          │
│  ┌──────┐ ┌──────┐ ┌──────┐        │
│  │Page 0│ │Page 1│ │Page 2│  ...   │
│  │16 slots│16 slots│16 slots│       │
│  └──────┘ └──────┘ └──────┘        │
└─────────────────────────────────────┘

Sequence A: [Page 0, Page 2]  ← non-contiguous, that's fine
Sequence B: [Page 1, Page 4]
</code></pre></div></div> <p>Each new token writes directly to the next slot — <strong>zero copies, zero allocations</strong>.</p> <h2 id="implementation">Implementation</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">class</span> <span class="nc">PagedKVCache</span><span class="p">:</span>
    <span class="k">def</span> <span class="nf">__init__</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">n_layers</span><span class="p">,</span> <span class="n">n_heads</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">,</span> <span class="n">page_size</span><span class="o">=</span><span class="mi">16</span><span class="p">,</span> <span class="n">max_pages</span><span class="o">=</span><span class="mi">256</span><span class="p">):</span>
        <span class="c1"># Pre-allocate everything upfront
</span>        <span class="n">self</span><span class="p">.</span><span class="n">k_pool</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">max_pages</span><span class="p">,</span> <span class="n">n_heads</span><span class="p">,</span> <span class="n">page_size</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">v_pool</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">zeros</span><span class="p">(</span><span class="n">max_pages</span><span class="p">,</span> <span class="n">n_heads</span><span class="p">,</span> <span class="n">page_size</span><span class="p">,</span> <span class="n">head_dim</span><span class="p">)</span>
        <span class="n">self</span><span class="p">.</span><span class="n">free_pages</span> <span class="o">=</span> <span class="nf">list</span><span class="p">(</span><span class="nf">range</span><span class="p">(</span><span class="n">max_pages</span><span class="p">))</span>

    <span class="k">def</span> <span class="nf">append</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">seq_id</span><span class="p">,</span> <span class="n">layer_idx</span><span class="p">,</span> <span class="n">new_k</span><span class="p">,</span> <span class="n">new_v</span><span class="p">):</span>
        <span class="n">slot</span> <span class="o">=</span> <span class="n">position</span> <span class="o">%</span> <span class="n">self</span><span class="p">.</span><span class="n">page_size</span>
        <span class="k">if</span> <span class="n">slot</span> <span class="o">==</span> <span class="mi">0</span><span class="p">:</span>  <span class="c1"># page full, grab a new one
</span>            <span class="n">page_id</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">free_pages</span><span class="p">.</span><span class="nf">pop</span><span class="p">(</span><span class="mi">0</span><span class="p">)</span>
        <span class="c1"># Write directly — no copy
</span>        <span class="n">self</span><span class="p">.</span><span class="n">k_pool</span><span class="p">[</span><span class="n">page_id</span><span class="p">,</span> <span class="p">:,</span> <span class="n">slot</span><span class="p">,</span> <span class="p">:]</span> <span class="o">=</span> <span class="n">new_k</span>
        <span class="n">self</span><span class="p">.</span><span class="n">v_pool</span><span class="p">[</span><span class="n">page_id</span><span class="p">,</span> <span class="p">:,</span> <span class="n">slot</span><span class="p">,</span> <span class="p">:]</span> <span class="o">=</span> <span class="n">new_v</span>
</code></pre></div></div> <h2 id="results">Results</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>torch.cat cache:  2.02s  |  49.4 tokens/sec
PagedAttention:   1.74s  |  57.4 tokens/sec
Speedup:          1.16x
</code></pre></div></div> <p>The advantage grows with sequence length since we avoid the O(n²) copy overhead.</p> <h2 id="why-this-matters">Why This Matters</h2> <p>This is exactly how <a href="https://github.com/vllm-project/vllm">vLLM</a> manages KV-cache memory in production. Understanding PagedAttention from scratch reveals why memory management is the key bottleneck in LLM serving — not compute.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><summary type="html"><![CDATA[Why torch.cat creates O(n²) memory copies, and how PagedAttention eliminates them with pre-allocated page pools.]]></summary></entry><entry><title type="html">KV-Cache: Why Naive Inference is O(n²) and How Caching Fixes It</title><link href="https://aserputov.github.io/blog/2026/kv-cache-from-scratch/" rel="alternate" type="text/html" title="KV-Cache: Why Naive Inference is O(n²) and How Caching Fixes It"/><published>2026-06-10T00:00:00+00:00</published><updated>2026-06-10T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/kv-cache-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/kv-cache-from-scratch/"><![CDATA[<h2 id="the-problem">The Problem</h2> <p>When generating text with a transformer, we produce one token at a time. Without optimization, generating token 100 means running the full model on all 100 tokens — even though tokens 1-99 haven’t changed.</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Step 1:  process [The]                    → predict "meaning"
Step 2:  process [The, meaning]           → predict "of"       ← recomputes "The"
Step 3:  process [The, meaning, of]       → predict "life"     ← recomputes both
...
Step 99: process [all 99 tokens]          → predict token 100  ← recomputes ALL
</code></pre></div></div> <p>Total attention computations: 1 + 2 + 3 + … + n = <strong>O(n²)</strong></p> <h2 id="the-insight">The Insight</h2> <p>In attention, we compute Q, K, V for each token. But K and V for past tokens <strong>never change</strong> — the causal mask means past tokens can’t see the future. So we cache them.</p> <h2 id="two-phases">Two Phases</h2> <table> <thead> <tr> <th>Phase</th> <th>Input</th> <th>Work</th> </tr> </thead> <tbody> <tr> <td><strong>Prefill</strong></td> <td>Entire prompt (N tokens)</td> <td>Process all at once, build KV-cache</td> </tr> <tr> <td><strong>Decode</strong></td> <td>1 new token</td> <td>Compute Q,K,V for new token only, reuse cached K,V</td> </tr> </tbody> </table> <h2 id="implementation">Implementation</h2> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">forward</span><span class="p">(</span><span class="n">self</span><span class="p">,</span> <span class="n">x</span><span class="p">,</span> <span class="n">kv_cache</span><span class="o">=</span><span class="bp">None</span><span class="p">):</span>
    <span class="n">q</span><span class="p">,</span> <span class="n">k</span><span class="p">,</span> <span class="n">v</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="nf">c_attn</span><span class="p">(</span><span class="n">x</span><span class="p">).</span><span class="nf">split</span><span class="p">(</span><span class="n">C</span><span class="p">,</span> <span class="n">dim</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>

    <span class="k">if</span> <span class="n">kv_cache</span> <span class="ow">is</span> <span class="ow">not</span> <span class="bp">None</span><span class="p">:</span>
        <span class="n">prev_k</span><span class="p">,</span> <span class="n">prev_v</span> <span class="o">=</span> <span class="n">kv_cache</span>
        <span class="n">k</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">prev_k</span><span class="p">,</span> <span class="n">k</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>  <span class="c1"># append new K to cached
</span>        <span class="n">v</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">cat</span><span class="p">([</span><span class="n">prev_v</span><span class="p">,</span> <span class="n">v</span><span class="p">],</span> <span class="n">dim</span><span class="o">=</span><span class="mi">2</span><span class="p">)</span>  <span class="c1"># append new V to cached
</span>
    <span class="n">new_cache</span> <span class="o">=</span> <span class="p">(</span><span class="n">k</span><span class="p">,</span> <span class="n">v</span><span class="p">)</span>
    <span class="n">scores</span> <span class="o">=</span> <span class="p">(</span><span class="n">q</span> <span class="o">@</span> <span class="n">k</span><span class="p">.</span><span class="n">T</span><span class="p">)</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">d</span><span class="p">)</span>
    <span class="k">return</span> <span class="n">output</span><span class="p">,</span> <span class="n">new_cache</span>
</code></pre></div></div> <h2 id="results">Results</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>No cache:   5.32s  |  18.8 tokens/sec
KV-cache:   2.02s  |  49.4 tokens/sec
Speedup:    2.63x
</code></pre></div></div> <p>The speedup grows with sequence length — longer generations benefit more.</p> <h2 id="the-trade-off">The Trade-off</h2> <p>KV-cache trades <strong>memory for speed</strong>. Each request stores O(n × layers × d_model) of cached tensors. This is why context length is expensive — longer context means bigger cache means more memory per request. Understanding this trade-off is fundamental to LLM serving systems like vLLM and TGI.</p> <h2 id="whats-next">What’s Next</h2> <p>The naive <code class="language-plaintext highlighter-rouge">torch.cat</code> approach has its own O(n²) problem — it copies the entire cache at every step. <a href="/blog/2026/pagedattention-from-scratch/">PagedAttention</a> solves this with pre-allocated memory pages.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/inference-engine">github.com/aserputov/inference-engine</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="inference"/><summary type="html"><![CDATA[How KV-cache reduces autoregressive generation from O(n²) to O(n), with from-scratch implementation and 2.6x speedup benchmarks.]]></summary></entry><entry><title type="html">GPT-2: Loading Real Weights and Generating Text</title><link href="https://aserputov.github.io/blog/2026/gpt2-from-scratch/" rel="alternate" type="text/html" title="GPT-2: Loading Real Weights and Generating Text"/><published>2026-05-18T00:00:00+00:00</published><updated>2026-05-18T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/gpt2-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/gpt2-from-scratch/"><![CDATA[<h2 id="the-goal">The Goal</h2> <p>Take OpenAI’s published GPT-2 weights (124M parameters), write the entire forward pass by hand, and generate real text. No HuggingFace, no abstractions — just matrix operations.</p> <h2 id="architecture">Architecture</h2> <p>GPT-2 is a decoder-only transformer. The full structure:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Token IDs
  → wte[token_id]  (token embedding, 50257 × 768)
  → + wpe[position] (position embedding, 1024 × 768)
  → 12 × Transformer Block
  → LayerNorm
  → logits = x @ wte.T  (weight tying)
  → sample next token
</code></pre></div></div> <p>Weight tying: the same embedding matrix used to convert token IDs to vectors is reused (transposed) to convert the final hidden state back to vocabulary logits. This means the model has fewer parameters and the embedding space stays consistent.</p> <h2 id="loading-the-weights">Loading the Weights</h2> <p>OpenAI published the weights as TensorFlow checkpoints. Loading them into a from-scratch PyTorch model means mapping every parameter name:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># OpenAI's naming → our model's structure
</span><span class="sh">"</span><span class="s">model/h0/attn/c_attn/w</span><span class="sh">"</span>  <span class="err">→</span> <span class="n">blocks</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">attn</span><span class="p">.</span><span class="n">c_attn</span><span class="p">.</span><span class="n">weight</span>
<span class="sh">"</span><span class="s">model/h0/mlp/c_fc/w</span><span class="sh">"</span>     <span class="err">→</span> <span class="n">blocks</span><span class="p">[</span><span class="mi">0</span><span class="p">].</span><span class="n">mlp</span><span class="p">.</span><span class="n">c_fc</span><span class="p">.</span><span class="n">weight</span>
<span class="c1"># ... 148 parameters total
</span></code></pre></div></div> <p>If any single weight is loaded wrong — transposed when it shouldn’t be, assigned to the wrong layer — the output is garbage. Getting coherent English text out confirms the implementation is correct.</p> <h2 id="gelu-activation">GELU Activation</h2> <p>GPT-2 uses GELU instead of ReLU in the feed-forward layers:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">def</span> <span class="nf">gelu</span><span class="p">(</span><span class="n">x</span><span class="p">):</span>
    <span class="k">return</span> <span class="mf">0.5</span> <span class="o">*</span> <span class="n">x</span> <span class="o">*</span> <span class="p">(</span><span class="mi">1</span> <span class="o">+</span> <span class="n">torch</span><span class="p">.</span><span class="nf">tanh</span><span class="p">(</span><span class="nf">sqrt</span><span class="p">(</span><span class="mi">2</span><span class="o">/</span><span class="n">pi</span><span class="p">)</span> <span class="o">*</span> <span class="p">(</span><span class="n">x</span> <span class="o">+</span> <span class="mf">0.044715</span> <span class="o">*</span> <span class="n">x</span><span class="o">**</span><span class="mi">3</span><span class="p">)))</span>
</code></pre></div></div> <p>Unlike ReLU which hard-clips at 0, GELU has a smooth curve that allows small negative values through. This means neurons don’t “die” as easily during training.</p> <h2 id="text-generation">Text Generation</h2> <p>The model predicts a probability distribution over the entire vocabulary for the next token. How you sample from that distribution controls the output:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">logits</span> <span class="o">=</span> <span class="nf">model</span><span class="p">(</span><span class="n">tokens</span><span class="p">)[:,</span> <span class="o">-</span><span class="mi">1</span><span class="p">,</span> <span class="p">:]</span>     <span class="c1"># last token's predictions
</span><span class="n">logits</span> <span class="o">=</span> <span class="n">logits</span> <span class="o">/</span> <span class="n">temperature</span>         <span class="c1"># sharpen or flatten distribution
</span>
<span class="c1"># Top-k: only consider the k most likely tokens
</span><span class="n">top_k_logits</span><span class="p">,</span> <span class="n">top_k_indices</span> <span class="o">=</span> <span class="n">torch</span><span class="p">.</span><span class="nf">topk</span><span class="p">(</span><span class="n">logits</span><span class="p">,</span> <span class="n">k</span><span class="o">=</span><span class="mi">40</span><span class="p">)</span>
<span class="n">probs</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">top_k_logits</span><span class="p">)</span>
<span class="n">next_token</span> <span class="o">=</span> <span class="n">top_k_indices</span><span class="p">[</span><span class="n">torch</span><span class="p">.</span><span class="nf">multinomial</span><span class="p">(</span><span class="n">probs</span><span class="p">,</span> <span class="mi">1</span><span class="p">)]</span>
</code></pre></div></div> <ul> <li><strong>Temperature &lt; 1</strong>: more deterministic, sticks to obvious completions</li> <li><strong>Temperature &gt; 1</strong>: more random, more creative/weird</li> <li><strong>Top-k</strong>: prevents sampling extremely unlikely tokens</li> </ul> <h2 id="what-it-produces">What It Produces</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Prompt: "The meaning of life is"
Output: "The meaning of life is not to be found in the
         pursuit of happiness, but in the quiet moments
         of reflection that come when we least expect them."
</code></pre></div></div> <p>Coherent English from raw matrix multiplications. Every operation — every attention head, every layer norm, every GELU — done by hand.</p> <h2 id="what-i-took-away">What I Took Away</h2> <p>GPT-2 is not complicated. It’s the same transformer block from the previous project, repeated 12 times, with specific weight shapes and an embedding/unembedding layer. The complexity is in the training (which OpenAI did) and in the inference optimizations needed to make it fast (which is the next project).</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/gpt2-from-scratch">github.com/aserputov/gpt2-from-scratch</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="transformer"/><summary type="html"><![CDATA[Writing GPT-2's forward pass from scratch, loading OpenAI's 124M weights, and generating coherent English text.]]></summary></entry><entry><title type="html">Transformer: Self-Attention From Scratch</title><link href="https://aserputov.github.io/blog/2026/transformer-from-scratch/" rel="alternate" type="text/html" title="Transformer: Self-Attention From Scratch"/><published>2026-04-20T00:00:00+00:00</published><updated>2026-04-20T00:00:00+00:00</updated><id>https://aserputov.github.io/blog/2026/transformer-from-scratch</id><content type="html" xml:base="https://aserputov.github.io/blog/2026/transformer-from-scratch/"><![CDATA[<h2 id="why-not-just-use-lstms">Why Not Just Use LSTMs?</h2> <p>LSTMs process sequences one token at a time. Token 100 has to wait for tokens 1-99 to finish. This is inherently sequential — you can’t parallelize it.</p> <p>The transformer (Vaswani et al., 2017) processes all tokens simultaneously. The key mechanism: <strong>self-attention</strong>.</p> <h2 id="self-attention">Self-Attention</h2> <p>Every token produces three vectors from the same input:</p> <ul> <li><strong>Query (Q)</strong>: what am I looking for?</li> <li><strong>Key (K)</strong>: what do I contain?</li> <li><strong>Value (V)</strong>: what information do I carry?</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">Q</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">W_q</span>  <span class="c1"># (T, d)
</span><span class="n">K</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">W_k</span>  <span class="c1"># (T, d)
</span><span class="n">V</span> <span class="o">=</span> <span class="n">x</span> <span class="o">@</span> <span class="n">W_v</span>  <span class="c1"># (T, d)
</span>
<span class="n">scores</span> <span class="o">=</span> <span class="n">Q</span> <span class="o">@</span> <span class="n">K</span><span class="p">.</span><span class="n">T</span> <span class="o">/</span> <span class="nf">sqrt</span><span class="p">(</span><span class="n">d</span><span class="p">)</span>  <span class="c1"># (T, T) — every token scores against every other
</span><span class="n">weights</span> <span class="o">=</span> <span class="nf">softmax</span><span class="p">(</span><span class="n">scores</span><span class="p">)</span>
<span class="n">output</span> <span class="o">=</span> <span class="n">weights</span> <span class="o">@</span> <span class="n">V</span>         <span class="c1"># weighted combination of values
</span></code></pre></div></div> <p>The T x T scores matrix is the core of the transformer. Each entry says “how much should token i attend to token j?”</p> <p>Division by <code class="language-plaintext highlighter-rouge">sqrt(d)</code> prevents the dot products from getting too large, which would push softmax into regions where the gradient is near zero.</p> <h2 id="multi-head-attention">Multi-Head Attention</h2> <p>Instead of one big attention, split into multiple heads that each look at a different subspace:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c1"># d_model=768, n_heads=12 → head_dim=64
# Each head learns different relationships:
# - one head might learn positional patterns
# - another might learn syntactic dependencies
# - another might track coreference
</span></code></pre></div></div> <p>The outputs of all heads get concatenated and projected back to d_model.</p> <h2 id="positional-encoding">Positional Encoding</h2> <p>Self-attention treats the input as a set — there’s no notion of position. “dog bites man” and “man bites dog” would produce the same output. Positional encoding (sin/cos at different frequencies) adds position information:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">PE</span><span class="p">(</span><span class="n">pos</span><span class="p">,</span> <span class="mi">2</span><span class="n">i</span><span class="p">)</span>   <span class="o">=</span> <span class="nf">sin</span><span class="p">(</span><span class="n">pos</span> <span class="o">/</span> <span class="mi">10000</span><span class="o">^</span><span class="p">(</span><span class="mi">2</span><span class="n">i</span><span class="o">/</span><span class="n">d</span><span class="p">))</span>
<span class="nc">PE</span><span class="p">(</span><span class="n">pos</span><span class="p">,</span> <span class="mi">2</span><span class="n">i</span><span class="o">+</span><span class="mi">1</span><span class="p">)</span> <span class="o">=</span> <span class="nf">cos</span><span class="p">(</span><span class="n">pos</span> <span class="o">/</span> <span class="mi">10000</span><span class="o">^</span><span class="p">(</span><span class="mi">2</span><span class="n">i</span><span class="o">/</span><span class="n">d</span><span class="p">))</span>
</code></pre></div></div> <h2 id="the-full-block">The Full Block</h2> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Input
  ↓
LayerNorm → Multi-Head Attention → + (residual)
  ↓
LayerNorm → Feed-Forward Network → + (residual)
  ↓
Output
</code></pre></div></div> <p>Residual connections let the gradient flow directly through addition. Layer normalization keeps activations stable. Stack 12 of these blocks and you get GPT-2.</p> <h2 id="ot2--the-cost">O(T^2) — The Cost</h2> <p>The scores matrix is T x T. For sequence length 1024, that’s ~1M entries. For 128K (modern LLMs), that’s 16 billion. This is why long context is expensive, and why Flash Attention exists — but that comes later.</p> <h2 id="what-i-took-away">What I Took Away</h2> <p>Building this made the Q/K/V mechanism intuitive instead of abstract. The transformer isn’t magic — it’s matrix multiplications with a clever structure that lets every token talk to every other token in one step, instead of relying on a hidden state to carry information across the whole sequence.</p> <p><strong>Code:</strong> <a href="https://github.com/aserputov/attention-from-scratch">github.com/aserputov/attention-from-scratch</a></p>]]></content><author><name></name></author><category term="from-scratch"/><category term="from-scratch"/><category term="transformer"/><summary type="html"><![CDATA[Building a transformer from scratch — Q/K/V attention, multi-head, positional encoding, and why it replaced RNNs.]]></summary></entry></feed>